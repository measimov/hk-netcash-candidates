# 部署说明

这是静态站点，可以直接拖拽上传到免费静态托管平台。

推荐：

1. Cloudflare Pages
   - 进入 Pages
   - Create using direct upload
   - 上传本目录或 `hk_public_site.zip`

2. Netlify Drop
   - 打开 https://app.netlify.com/drop
   - 拖拽本目录或 zip

入口文件是 `index.html`。

说明：公开包不包含 47MB 的披露易原始标题大表，只保留页面和已汇总的 CSV。
