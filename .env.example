# Copy to .env locally if your shell loader uses dotenv.
# Never commit real keys.
TUSHARE_TOKEN=replace_with_local_tushare_token
DPSK_API_KEY=replace_with_local_dpsk_key

# Optional DeepSeek-compatible endpoint override.
# DPSK_BASE_URL=https://api.deepseek.com/chat/completions
